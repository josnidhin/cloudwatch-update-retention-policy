# Cloudwatch-update-retention-policy

A simple script to update retention policy of all the cloudwatch log groups

# Usage

```
$ npm install
$ AWS_REGION=<aws region> RETENTION_PERIOD=<number of days> npm start
```
